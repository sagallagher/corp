#ifndef _STAR_GUARD_
#define  _STAR_GUARD_
#include "../../CORP/Matrix/Matrix.hpp"
#include <iostream>

template<typename S>
class Star
{
    unsigned _rows;
    unsigned _cols;
    //new matrix
    void newStar(const unsigned _rows, const unsigned _cols)
    {
    Matrix<T>* _star = new Matrix<T>;
    star[_rows][_cols];
    ~Matrix();
    }
    ~Star();
};

Star<S>::

#endif
